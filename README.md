# QR-Code-Generator
The QR Code Generator is a Node.js application that creates QR codes from user-entered URLs. It uses Inquirer to take input, qr-image to generate the QR code, and the fs module to save it as a PNG file. This project demonstrates user input handling, package usage, and file operations in Node.js.
